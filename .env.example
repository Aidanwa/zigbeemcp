# MQTT
MQTT_HOST=localhost
MQTT_PORT=1883
MQTT_USERNAME=
MQTT_PASSWORD=
# Devices (optional filter)
Z2M_DEVICES=bedroom_bulb_1,bedroom_bulb_2

# Paths (optional overrides)
DB_PATH=./data/smarthome.sqlite3

# API Server
API_HOST=0.0.0.0
API_PORT=8000
# REQUIRED: Comma-separated list of valid API keys
API_KEYS=your-secret-api-key-here,another-key-if-needed
# Optional: CORS origins for web frontends (comma-separated)
API_CORS_ORIGINS=
# Device state confirmation timeout (seconds)
API_DEVICE_STATE_TIMEOUT=5.0
